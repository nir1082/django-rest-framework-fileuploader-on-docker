# django-rest-framework-fileuploader-on-docker
DockerでDjangoRestFrameworkのファイルアップローダーアプリを建てる

### コマンド
sudo docker build -t [IMAGE_TAG] .

sudo docker run --name [CONTAINER_NAME] -d -p [PORT]:80 [IMAGE_TAG]
